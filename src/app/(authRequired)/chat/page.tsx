'use client';
import Chat from '@/components/chat';

const MenuLayout = () => {
  return (
    <div>
      {/* Mostrar el chat en tiempo real */}
      <Chat />
    </div>
  );
};

export default MenuLayout;
